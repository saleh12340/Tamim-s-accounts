# Project-specific ProGuard/R8 rules.
# Release builds currently use minifyEnabled = false, so no custom
# shrinking rules are required. This file is kept because the Android
# release build configuration references it.
